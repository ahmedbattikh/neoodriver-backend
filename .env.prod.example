APP_ENV=prod
APP_DEBUG=0
APP_SECRET=change_me
DEFAULT_URI=https://example.com
DEPLOY_TAG=dev

# Database (adjust as needed)
DATABASE_URL="mysql://project:project@mysql:3306/project?serverVersion=8.0&charset=utf8mb4"

# MySQL service variables (used by compose)
MYSQL_DATABASE=project
MYSQL_USER=project
MYSQL_PASSWORD=project
MYSQL_VERSION=8.0
MYSQL_CHARSET=utf8mb4
MYSQL_ROOT_PASSWORD=!ChangeMeRoot!

# Caddy/FrankenPHP
SERVER_NAME=example.com
## Mercure removed; no Mercure-specific variables required

# phpMyAdmin
PMA_USER=neooadmin
PMA_PASSWORD=NeooDriver@9090

# Mailer (optional)
# MAILER_DSN=smtp://mailhog:1025

# Add other production secrets here (JWT keys, API keys, etc.)
JWT_PASSPHRASE=ChangeMeStrongPassphrase
JWT_TTL=3600
JWT_REFRESH_TTL=2592000